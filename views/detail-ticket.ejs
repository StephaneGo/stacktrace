<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Modifier un ticket</title>
    <link rel="stylesheet" href="/styles/my-tailwind.css" />
  </head>
  <body>
    <%- include('_header.ejs') %>

    <h2 class="mx-auto w-fit mb-5 text-4xl"><%= ticket.titre %></h2>
    <div class="flex flex-col items-center">
      <section class="">
        <h3 class="text-md text-gray-600 leading-7">Description :</h3>
        <p class="mb-4"><%= ticket.description %></p>
        <div class="flex gap-1">
          <div
            class="rounded-2xl w-fit px-3 py-0.5 text-gray-50 bg-indigo-500 text-sm"
          >
            <a href="/tickets/<%= ticket.id %>/modifier">Modifier</a>
          </div>
          <div
            class="rounded-2xl w-fit px-3 py-0.5 text-gray-50 bg-indigo-500 text-sm"
          >
            <a href="/tickets/<%= ticket.id %>/supprimer">Supprimer</a>
          </div>
        </div>
      </section>
    </div>
  </body>
</html>
